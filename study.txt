GREAT-PVT / PRIDE PPP-AR 算法优化借鉴方案（适配 rtklib-java / 车载测绘 / 城市峡谷）
含项目现状交叉审核与落地修订

================================================================================
0. 方案总览
================================================================================

本方案基于武汉大学 GREAT-PVT、PRIDE PPP-AR 两大科研级开源软件，针对 rtklib-java
现有算法短板，结合车载移动测绘、城市峡谷高精度RTK、PPP高精度后处理三类核心场景，
完成系统化算法选型、场景边界界定、优先级规划与工程落地规范。同时结合项目现有存量
优化能力，完成交叉比对、冲突修正、优先级重排与风险整改，形成适配当前代码基线的
最终落地版本。

方案核心原则：
- 不重构现有EKF架构、不盲目引入因子图
- 严格区分RTK/PPP算法适用性
- 测绘场景可靠性优先于固定率
- 新旧算法互补兼容、杜绝功能冲突与裸奔上线
- 先RTK后PPP，两阶段独立交付

================================================================================
1. 项目定位与关键概念勘误
================================================================================

1.1 三大项目技术定位对比
─────────────────────────────────────────────────────────────────────
项目                  技术架构                    核心能力                          本次借鉴定位
─────────────────────────────────────────────────────────────────────
GREAT-PVT            传统滤波/批量最小二乘       多频逐级AR、部分AR、              核心参考源，
                     （无FGO因子图）             Bootstrapping判据、               全算法兼容EKF
                                                 全套高精度误差模型
PRIDE PPP-AR         Fortran/C高精度后处理       残差编辑质控、BDS码偏差、         误差质控、PPP
                     框架                       AI-AR、MHM多路径、                高精度模块参考
                                                 高精度对流层/潮汐
rtklib-java          标准EKF+经典最小二乘        基础RTK/PPP解算，                 主体工程载体，
                                                 架构稳定、含多项自研优化          算法可插拔升级
GraphRTK-INS         因子图滑窗优化(FGO)        GNSS/IMU紧耦合、                  远期备选，
(GREAT-PIFGO)                                   短时遮挡轨迹连续                  不纳入当前迭代
─────────────────────────────────────────────────────────────────────

1.2 重要勘误
─────────────────────────────────────────────────────────────────────
1）GREAT-PVT 不含因子图优化：其所有模糊度固定策略、误差模型均可在标准EKF框架下
   运行，无需重构状态估计。
2）因子图、滑窗IMU紧耦合属于 GraphRTK-INS，为独立仓库，不属于 GREAT-PVT，
   当前阶段不落地。
3）本次所有P0/P1/P2优化，均为算法层升级，与FGO无关，兼容性、工程可控性极强。

1.3 业务场景约束规则
─────────────────────────────────────────────────────────────────────
1）车载测绘场景：可靠性优先，宁可不固定、不跳变，禁止假固定输出不合格厘米级成果；
   基线多为短基线（数公里内）。
2）城市峡谷场景：多路径、周跳频发、卫星断续，需强化弧段连续性、鲁棒质控、
   自适应AR策略。
3）PPP后处理场景：追求毫米级稳态精度，依赖高精度对流层、潮汐、偏差模型与
   AR智能校验。

1.4 项目存量自研优化汇总（全部保留、新旧互补）
─────────────────────────────────────────────────────────────────────
本方案新增算法均与以下项目独有优化正交互补、可叠加启用，不替换、不冲突，
全部纳入长期维护体系：

1）enableAmbAnchor：模糊度锚固保护，稳定历史固定解，与全新Partial AR前后互补
2）enableParRefReselect：参考星重选，为弱化版子集优化，全新Partial AR实现后
   可作为兜底策略
3）enableAdaptiveQ：运动状态自适应位置过程噪声，与参数类型级噪声模型互补叠加
4）enableIggiii：观测域IGGIII抗差降权，与PRIDE弧段域残差编辑互补
5）enableSnrMedian：SNR中值参考星选择，异构接收机网络场景有价值
6）atmFrozenNsThresh：大气冻结阈值，城市峡谷少星场景刚需
7）enableIonoTropGradient：电离层+对流层梯度估计，已实现，长基线RTK必需
8）BaseStationMonitor：基站稳定性监测，工程运维刚需，与算法优化正交
9）rtklib-adjust P0/P1/P3：基线质量加权/残差提取/H矩阵提取，平差模块独有

================================================================================
2. RTK阶段优化方案（先行交付）
================================================================================

2.1 逐级模糊度固定 EWL→WL→NL（P0 最高优先级）
─────────────────────────────────────────────────────────────────────
来源：      GREAT-PVT arsig 模块
目的：      利用多频波长差异逐级约束固定模糊度，提升RTK固定连续性和精度
现状短板：  仅单步窄巷LAMBDA+Ratio，多频利用率低
改造类型：  核心算法重构（resamb_LAMBDA()）
侵入点：    RtkProcessor.resamb_LAMBDA()
回退方案：  Config开关 enableCascadeAR=false时退回单步LAMBDA
依赖：      多频数据（≥3频），项目已支持NFREQ=6

逐级策略：
  EWL级：超宽巷（λ_EWL ≈ 0.86m），极易取整，Ratio阈值可放宽至1.5
  WL级： 宽巷（λ_WL ≈ 0.86m GPS L1/L2），受EWL约束后搜索空间缩小
  NL级： 窄巷（λ_NL ≈ 0.10m），受WL约束完成最终固定

与现有锚固机制的兼容：
  - EWL级：超宽巷极易取整，锚固意义不大，但仍需独立Ratio校验
  - WL级：宽巷固定结果约束NL，锚固可保护已稳定的WL模糊度
  - NL级：窄巷是最终精度，锚固最关键
  - 逐级AR的每一级都应支持锚固分离，但锚固计数仅在NL级管理
  - 注意：EWL/WL级虽然"极易取整"，但不能跳过验证，错误固定会污染NL级

分阶段落地：
  Phase 1：双频WL→NL（覆盖大多数双频接收机）
  Phase 2：三频EWL→WL→NL（完整三频逐级）

2.2 精细化残差编辑与周跳检测（P0，从P1提升）
─────────────────────────────────────────────────────────────────────
来源：      PRIDE redig 全套模块
目的：      通过残差时序迭代跳变检测等手段保障模糊度弧段连续
改造类型：  新增模块（弧段域质控）
侵入点：    udbias() 周跳处理 + 新增 ResidualEditor 类
回退方案：  Config开关 enableResidualEdit=false时使用原生周跳检测
优先级提升理由：所有高级AR策略的底层基础，周跳检测不可靠则逐级AR和Partial AR失效

现状短板：
  - udbias()中仅有基本周跳处理（LLI标记 + 几何距离跳变）
  - 缺少残差时序迭代检测
  - 缺少伪距-载波一致性校验
  - 缺少弧段完整性筛查

与IGGIII的关系：
  - enableIggiii：观测域降权抑制粗差（单历元）
  - PRIDE残差编辑：弧段域检测周跳和异常（多历元时序）
  - 两者作用层面不同，互补

2.3 部分模糊度固定 Partial AR（P1）
─────────────────────────────────────────────────────────────────────
来源：      GREAT-PVT arsig 模块
目的：      全局Ratio失败后，剔除劣质模糊度维度，保留可信子集完成固定
改造类型：  核心算法新增（LAMBDA子集搜索）
侵入点：    RtkProcessor.resamb_LAMBDA() Ratio检验失败后
回退方案：  Config开关 enablePartialAR=false时退回全局Ratio

与现有机制的关系：
  - enableAmbAnchor：保护"过去已稳定"的模糊度（向后保护）→ 互补
  - enableParRefReselect：仅排除参考星，非真正子集选择 → 弱化版，保留作兜底
  - Partial AR：在当前历元"剔除当前劣质"维度（向前筛选）

安全约束：
  - 必须三判据联合校验：Ratio + Bootstrapping成功率 + 残差时序连续性
  - 缺一不可，否则假固定风险不可控
  - 测绘场景宁可失锁不输出错误解

2.4 Bootstrapping成功率联合判据（P1，从P2提升）
─────────────────────────────────────────────────────────────────────
来源：      GREAT-PVT arsig 模块
目的：      作为Ratio的互补判据，与Ratio、残差时序联合判定模糊度固定可靠性
改造类型：  新增判据模块
侵入点：    resamb_LAMBDA() 固定判据处
回退方案：  Config开关 enableBootstrapping=false时仅用Ratio
优先级提升理由：Partial AR安全落地的必要条件，没有多判据保护就是"裸奔"

现状：
  - 单一Ratio判据，参数敏感性分析显示thresar从2.0→6.0 FIX率下降14.9%
  - Ratio判据对阈值敏感，需要互补判据降低敏感性

2.5 BDS卫星码偏差改正（P2）
─────────────────────────────────────────────────────────────────────
来源：      PRIDE PPP-AR（Wanninger模型，2020 IEEE CBD论文）
目的：      针对BDS卫星，特别是GEO卫星和低高度角IGSO/MEO卫星的码偏差进行改正
改造类型：  新增改正模块
侵入点：    观测值改正阶段
回退方案：  Config开关 enableBdsCodeBias=false时跳过

智能开关策略：
  - RTK短基线默认关闭（双差可大部分抵消）
  - GEO卫星或低高度角时自动开启
  - 参考数据：BDS宽巷模糊度固定成功率可提升37.6%

BDS-3新信号注意：
  - B1C/B2a的码偏差特性在RTKLIB 2.5.0中可能未覆盖
  - 需先确认rtkcmn.c中的频率定义和码偏差处理是否完整

2.6 GPT3+VMF3对流层模型（RTK场景降为P1/P2，PPP场景P0）
─────────────────────────────────────────────────────────────────────
来源：      PRIDE PPP-AR
目的：      提升中长基线RTK和PPP的对流层改正精度
改造类型：  新增模型替换
侵入点：    对流层改正函数
回退方案：  Config开关 tropoModel=GPT3_VMF3 / SAASTAMOINEN 切换

优先级调整理由：
  - 项目实测短基线（<10km）Saastamoinen FIX率81.0%，EST模式仅40.4%
  - GPT3+VMF3在短基线RTK收益有限
  - 真正价值在中长基线（>10km）和PPP
  - RTK场景降为P1/P2，PPP场景保持P0

工程量：需嵌入1°×1° GPT3网格数据和VMF3球谐系数，数据文件约数MB-数十MB

AOD数据下载：
  - VMF3需要AOD（.grib2或.netcdf格式的数值天气预报数据
  - 可通过ProductDownloader扩展支持AOD自动下载
  - 或使用GPT3纯经验模型（无需AOD，精度略降但仍优于GMF）

2.7 自适应随机游走过程噪声（P2）
─────────────────────────────────────────────────────────────────────
来源：      PRIDE PPP-AR
目的：      按物理参数区分噪声模型，提升解算稳健性
改造类型：  过程噪声模型重构
侵入点：    EKF状态转移噪声矩阵构建
回退方案：  Config开关 enableParamTypeNoise=false时使用统一prn[i]²*|tt|

与现有enableAdaptiveQ的关系：
  - study方案：参数类型级（ZTD用随机游走、钟差用白噪声、位置不使用随机游走）
  - 现有enableAdaptiveQ：运动状态级（位置过程噪声缩放，静态压制/动态放大）
  - 两者互补，可叠加

================================================================================
3. PPP阶段优化方案（RTK交付后启动）
================================================================================

3.1 GPT3+VMF3对流层模型（PPP场景P0）
─────────────────────────────────────────────────────────────────────
见§2.6，PPP场景下为P0最高优先级，精度提升30%~50%

3.2 IERS2010潮汐模型（P1）
─────────────────────────────────────────────────────────────────────
来源：      PRIDE PPP-AR
目的：      替换IERS2003，新增多种潮汐改正，满足毫米级精度要求
改造类型：  模型升级（Tides.java已有IERS2003框架）
侵入点：    Tides.java
回退方案：  Config开关 tidesModel=IERS2010 / IERS2003 切换

现状：
  - 项目实测短基线潮汐改正对FIX率影响0%
  - 对毫米级精度有价值，主要赋能后处理PPP
  - 改造量不大（Tides.java已有框架）

3.3 ISB/IFCB/IFB全维度偏差模型（P1）
─────────────────────────────────────────────────────────────────────
来源：      PRIDE PPP-AR
目的：      PPP非差解算中必须估计的偏差，提升多系统、多频高精度PPP精度
改造类型：  新增偏差参数估计
侵入点：    PPP状态扩展 + 观测方程
回退方案：  Config开关，RTK模块禁止调用

强约束：双差RTK天然抵消，禁止引入。如果在RTK中估计ISB/IFB，会引入冗余参数
降低FIX率。严格限定在PPP模块内实现。

3.4 PPP模糊度固定策略（P1）
─────────────────────────────────────────────────────────────────────
来源：      PRIDE PPP-AR（整数宽巷+窄巷两步固定）
目的：      PPP非差模糊度固定，实现PPP-AR
改造类型：  核心算法新增
侵入点：    PppProcessor
回退方案：  Config开关 enablePppAR=false时PPP浮点解

3.5 AI辅助AR验证模型（远期储备，不纳入当前迭代）
─────────────────────────────────────────────────────────────────────
来源：      PRIDE PPP-AR（XGBoost模型）
现状：      需8个特征输入，通过C/Fortran混合编程实现
评估：      属于PPP阶段远期储备。从算法角度评估，rtklib以经典EKF为基础，
           AI模型引入需谨慎评估功效与维护成本。RTKLIB原版未借鉴，有原因。
           暂不纳入当前迭代，待PPP阶段稳定后再评估。

3.6 精密产品下载模块（辅助工具，方便测试）
─────────────────────────────────────────────────────────────────────
目的：PPP解算需要精密轨道(SP3)、精密钟差(CLK)、地球自转参数(ERP)、DCB等精密产品，
      手动下载繁琐且易出错，需自动化下载模块方便测试和开发。
参考：PRIDE PPP-AR pdp3.sh PrepareProducts()函数，支持多源URL自动切换+缓存+解压
现状：rtklib-java已有Sp3Reader.java（SP3读取）、ClkReader.java（CLK读取）、
      PcvReader.java（PCV读取）、DcbReader.java（DCB读取）、OtlReader.java（OTL读取），
      但缺少自动下载功能，需手动准备精密产品文件。

需下载的精密产品及URL模式（参考PRIDE + IGS标准路径）：
1）精密轨道 SP3：
   - WUM0MGXRAP_YYYYDDD0000_01D_05M_ORB.SP3.gz（WUM超快速/快速，5min采样）
   - IGS0MGXRAP_YYYYDDD0000_01D_05M_ORB.SP3.gz（IGS MGEX快速）
   - IGS2R03FIN_YYYYDDD0000_01D_05M_ORB.SP3.gz（IGS最终，WHU镜像）
   - WUM0MGXRTS_YYYYDDD0000_01D_05M_ORB.SP3.gz（WUM实时，RAP失败回退）
2）精密钟差 CLK：
   - WUM0MGXRAP_YYYYDDD0000_01D_30S_CLK.CLK.gz（30s采样）
   - IGS2R03FIN_YYYYDDD0000_01D_30S_CLK.CLK.gz（IGS最终）
   - WUM0MGXRTS_YYYYDDD0000_01D_05S_CLK.CLK.gz（WUM实时，5s采样）
3）地球自转参数 ERP：
   - WUM0MGXRAP_YYYYDDD0000_01D_01D_ERP.ERP.gz
4）DCB/BIA码偏差：
   - CAS0MGXRAP_YYYYDDD0000_01D_01D_BIA.BIA.gz
5）ANTEX天线相位中心改正：
   - igs14.atx（IGS14框架天线绝对相位中心改正）
6）ERP（IGS最终）：
   - igs{WWWW}.erp.Z（WWWW为GPS周）
7）闰秒表：
   - leapsecond.txt

实现方案：
- 独立Maven模块 rtklib-product（与rtklib-core、rtklib-adjust平级）
- 主类 org.rtklib.java.product.ProductDownloader
- 依赖：仅slf4j+logback，不依赖rtklib-core，零第三方依赖
- 输入：日期范围（起止年月日）、产品类型、本地缓存目录
- 输出：下载到本地的SP3/CLK/ERP/DCB文件路径
- 功能：
  1) 根据日期自动生成产品文件名和URL（支持IGS/WUM/CAS多分析中心）
  2) 多源URL自动切换（主源失败→备选源→实时产品）
  3) 本地缓存（已下载不重复下载，支持.gz/.Z自动解压）
  4) 跨日产品自动合并（跨日处理需合并前后两天的SP3/CLK）
  5) 进度显示和错误提示
- URL优先级（与PRIDE-PPPAR pdp3.sh一致）：
  1) bdspride.com（PRIDE主源，国内高速，FTPS→HTTPS）
  2) igs.ign.fr（IGS官方MGEX）
  3) igs.gnsswhu.cn（WHU镜像，国内备选）
  4) RTS实时产品（RAP失败后回退）
- 默认缓存目录：{user.dir}/product/（与PRIDE-PPPAR一致）
- 默认URL基础地址：可通过构造器配置覆盖
- 缓存目录结构（与PRIDE一致）：
    product/
    ├── orbit/     SP3精密轨道
    ├── clock/     CLK精密钟差
    ├── erp/       ERP地球自转参数
    ├── dcb/       DCB/BIA码偏差
    └── table/     ANTEX/leapsec等静态表文件
- 所有日期/时间均基于UTC：
  - 输入的LocalDate视为UTC日期（GPS时与UTC差≤18s，对按日产品无影响）
  - DOY（年积日）按UTC日计算
  - GPS周从1980-01-06(UTC)起算
  - IGS产品文件名中的YYYYDDD均为UTC日期
- 定位：独立辅助工具模块，不依赖rtklib-core，后续可移走独立为命令行工具
- 优先级：中（PPP测试前必须可用，RTK阶段不需要）
- 状态：✅ 已实现，16个单元测试全部通过

3.7 PPP阶段远期储备（不纳入当前迭代）
─────────────────────────────────────────────────────────────────────
1）GPT3+VMF3用于中长基线RTK：RTK阶段不落地，PPP阶段验证后可回灌RTK
2）AI-AR验证模型（XGBoost）：需评估功效与维护成本，待PPP稳定后评估
3）MHM多路径改正：PRIDE特色，需长期观测数据建模，远期储备
4）卫星姿态文件（ATT.OBX）：PPP-AR需要，远期储备
5）FCB相位偏差文件：PPP宽巷/窄巷固定需要，远期储备

================================================================================
4. 逐项交叉审核讨论
================================================================================

4.1 逐级模糊度固定 EWL→WL→NL vs 现有
─────────────────────────────────────────────────────────────────────
维度        study.txt              项目现状
存在性      ✅ P0最高优先级         ❌ 完全未实现
现状短板    仅单步窄巷LAMBDA+Ratio  确认：原生仅LAMBDA+Ratio
依赖        多频数据（≥3频）        已支持NFREQ=6，nf=3时FIX率86.4%

讨论：这是价值最高、改造量最大的优化项。关键风险：逐级固定需要重构
resamb_LAMBDA()的核心逻辑，当前该函数已包含锚固分离（enableAmbAnchor），
改造需兼容。

4.2 部分模糊度固定 Partial AR vs 现有
─────────────────────────────────────────────────────────────────────
维度        study.txt              项目现状
概念        全局Ratio失败后剔除     已有两套相关但不同的机制
            劣质维度保留可信子集
现有1       —                      enableAmbAnchor：锚固已稳定模糊度
现有2       —                      enableParRefReselect：LAMBDA失败时重选参考星

讨论：Partial AR与AmbAnchor互补而非替代：
  - AmbAnchor：保护"过去已稳定"的模糊度（向后保护）
  - Partial AR：在当前历元"剔除当前劣质"维度（向前筛选）
  - enableParRefReselect是Partial AR的弱化版本——只排除参考星，不剔除任意劣质维度

风险管控：Partial AR的假固定风险需要多判据联合校验（Ratio+Bootstrapping+残差时序），
当前项目仅有Ratio判据。必须配套实现Bootstrapping成功率判据，否则假固定风险不可控。

4.3 精细化残差编辑+周跳检测 vs 现有
─────────────────────────────────────────────────────────────────────
维度        study.txt              项目现状
来源        PRIDE redig全套模块     C版原生MW+LG组合
现状短板    漏检误检周跳，          文档未明确提及周跳检测增强
            模糊度弧段频繁断裂

讨论：这是所有高级AR策略的底层基础。如果周跳检测不可靠，逐级AR和Partial AR
都会失效。优先级应从P1提升到P0。

与IGGIII互补：enableIggiii在观测域降权抑制粗差，PRIDE残差编辑在弧段域检测周跳，
两者作用层面不同。

4.4 Bootstrapping成功率联合判据 vs 现有
─────────────────────────────────────────────────────────────────────
维度        study.txt              项目现状
存在性      ✅ P2                   ❌ 完全未实现
现状        单一Ratio判据           确认仅有Ratio

讨论：当前项目参数敏感性分析显示thresar从2.0→6.0 FIX率下降14.9%，说明Ratio
判据对阈值敏感。应随Partial AR同步实现，不能延后EOP2。Partial AR没有多判据
保护就是"裸奔"。优先级从P2提升到P1。

4.5 BDS码偏差改正 vs 现有
─────────────────────────────────────────────────────────────────────
维度        study.txt              项目现状
存在性      ✅ Wanninger模型        ❌ 未实现
场景策略    RTK默认关闭，           无
            GEO/低高度角时自动开启

讨论：参考数据BDS宽巷模糊度固定成功率可提升37.6%。项目nf=3时FIX率86.4%，
还有提升空间。智能开关策略合理。需同时考虑BDS-3新信号（B1C/B2a）的码偏差"偏差"特性。

4.6 GPT3+VMF3对流层模型 vs 现有
─────────────────────────────────────────────────────────────────────
维度        study.txt              项目现状
存在性      ✅ P0最高优先级         ❌ 未实现
现有模型    —                      Saastamoinen+GMF
场景收益    中长基线RTK、           短基线SAASTAMOINEN比EST好+40.6%
            PPP精度提升30%~50%

讨论：关键矛盾——项目实测短基线Saastamoinen FIX率81.0%，EST模式仅40.4%。
GPT3+VMF3在短基线收益有限。P0优先级仅对PPP场景合理，RTK场景应降为P1/P2。

4.7 IERS2010潮汐模型 vs 现有
─────────────────────────────────────────────────────────────────────
维度        study.txt              项目现状
存在性      ✅ 毫米级IERS2003→2010  已有Tides.java（IERS#2003）
实测影响    毫米级精度提升          短基线潮汐对FIX率影响0%

讨论：短基线FIX率无影响，但对毫米级精度有价值，主要赋能后处理PPP。P1优先级合理，
改造量不大。

4.8 ISB/IFCB/IFB偏差模型 vs 现有
─────────────────────────────────────────────────────────────────────
维度        study.txt              项目现状
存在性      ✅ PPP专属             ❌ 未实现
强约束      双差RTK禁止引入        项目无此参数

讨论："双差RTK天然抵消，禁止引入"约束非常正确。严格限定在PPP模块内实现。

4.9 自适应随机游走过程噪声 vs 现有enableAdaptiveQ
─────────────────────────────────────────────────────────────────────
维度        study.txt              项目现状
概念        按物理参数区分噪声模型  滑动窗自适应Q矩阵
现有实现    —                      enableAdaptiveQ：Sigmoid映射+零速检测
差异        ZTD随机游走、           位置过程噪声缩放
            钟差白噪声、
            位置不使用随机游走

讨论：两者互补。study方案更物理合理（当前项目所有参数统一使用prn[i]²*|tt|），
现有enableAdaptiveQ处理位置噪声，新增参数类型级噪声模型配置，两者可叠加。

4.10 现有优化项在study.txt中的定位
─────────────────────────────────────────────────────────────────────
现有优化项                study.txt对应                  差距评估
─────────────────────────────────────────────────────────────────────
enableAdaptiveQ           P2自适应过程噪声（部分覆盖）   study更全面，现有仅位置级
enableAmbAnchor           P1 Partial AR（弱化版）        现有仅保护已固定模糊度
enableParRefReselect      P1 Partial AR（极弱化版）      仅排除参考星
atmFrozenNsThresh         无直接对应                    现有独有，应保留
enableIggiii              P1残差编辑（部分覆盖）        IGGIII观测域，PRIDE弧段域，互补
enableSnrMedian           无直接对应                    现有独有，应保留
enableIonoTropGradient    隐含在长基线RTK需求中         现有已实现
BaseStationMonitor        无直接对应                    现有独有，工程价值高

================================================================================
5. 关键风险与整改
================================================================================

5.1 Partial AR假固定风险（最高风险）
─────────────────────────────────────────────────────────────────────
收益风险同源。当前项目缺少多判据校验体系：

判据                  study要求    项目现状
─────────────────────────────────────────────────────────────────────
Ratio检验              ✅           ✅ 已有
Bootstrapping成功率    ✅           ❌ 缺失
残差时序连续性         ✅           ❌ 缺失

整改：Partial AR必须三判据联合，缺一不可。Bootstrapping从P2提升到与Partial AR
同批次实现。

5.2 优化项测试验证不足
─────────────────────────────────────────────────────────────────────
现有优化测试报告显示：所有优化项在高质量短基线数据上效果完全相同（FIX率94.5%无变化）。
说明：
  - 现有优化项缺少困难场景测试数据（城市峡谷、多路径、长基线）
  - study.txt的优化项同样需要困难场景验证
  - 参数敏感性分析仅覆盖基础PrcOpt参数，未覆盖RtkConfig优化项参数

建议建立分级测试体系：
  Level 1：高质量短基线（回归验证，确保不退化）
  Level 2：城市峡谷/多路径数据（验证IGGIII、Partial AR、周跳检测）
            需带IMU和不带IMU数据分别测试（GraphRTK-INS紧耦合备选）
  Level 3：中长基线（验证GPT3/VMF3、梯度参数、ISB/IFB）
  Level 4：PPP后处理（验证GPT3/VMF3、IERS2010、潮汐）

5.3 IGGIII参数默认值与文档不一致
─────────────────────────────────────────────────────────────────────
代码中RtkConfig.java的IGGIII默认值：
  iggiiiK0 = 3.0;   // 文档说1.5
  iggiiiK1 = 6.0;   // 文档说3.0
  iggiiiMinW = 0.5;  // 文档说1e-4

文档RTK_Extra_Optimizations.md中写的是K0=1.5, K1=3.0, minW=1e-4，这是标准IGGIII
参数。代码中的值偏宽松，可能导致抗差效果不足。

整改：统一为文档值，或明确说明代码值的选择依据。

5.4 逐级AR与现有锚固机制的兼容
─────────────────────────────────────────────────────────────────────
当前resamb_LAMBDA()已实现锚固/自由分离逻辑。逐级AR（EWL→WL→NL）需要在每一级
都执行LAMBDA搜索，与锚固分离的交互需要仔细设计。

关键注意：EWL和WL的固定结果本身也是整数，如果EWL/WL级出现错误固定，会污染NL级
的搜索。所以EWL/WL级虽然"锚固意义不大"，但仍需要独立的Ratio或Bootstrapping校验，
不能因为"极易取整"就跳过验证。

================================================================================
6. 优先级调整与落地修订
================================================================================

6.1 最终优先级（经交叉审核修订）
─────────────────────────────────────────────────────────────────────
调整   优化项                     原优先级   建议优先级   理由
─────────────────────────────────────────────────────────────────────
→      逐级模糊度固定EWL→WL→NL    P0         P0           核心价值，保持
↑      精细化周跳检测             P1         P0           所有AR策略的底层基础
→      部分模糊度固定Partial AR   P1         P1           需配套Bootstrapping
↑      Bootstrapping判据          P2         P1           Partial AR安全落地必要条件
→      BDS码偏差改正             P2         P2           保持
↓      GPT3+VMF3(RTK场景)        P0         P1/P2        短基线RTK收益有限
→      GPT3+VMF3(PPP场景)        P0         P0           PPP核心提升，保持
→      IERS2010潮汐              P1         P1           改造量不大，保持
→      ISB/IFCB/IFB              P1         P1           PPP专属，保持
→      自适应过程噪声            P2         P2           保持
新增   困难场景测试体系           —          P0           验证所有优化项实际效果

6.2 两阶段交付计划
─────────────────────────────────────────────────────────────────────
RTK阶段（先行交付）：
  P0：逐级模糊度固定 + 精细化周跳检测 + 困难场景测试体系
  P1：部分模糊度固定 + Bootstrapping判据
  P2：BDS码偏差改正 + GPT3+VMF3(RTK场景) + 自适应过程噪声

PPP阶段（RTK交付后启动）：
  P0：GPT3+VMF3(PPP场景)
  P1：IERS2010潮汐 + ISB/IFCB/IFB + PPP模糊度固定
  远期：AI-AR验证模型 + MHM多路径

6.3 时长与风险评估
─────────────────────────────────────────────────────────────────────
优化项                     预估工时    风险等级   主要风险
─────────────────────────────────────────────────────────────────────
逐级模糊度固定             3-4周       高         重构resamb_LAMBDA，兼容锚固
精细化周跳检测             2-3周       中         新增模块，不影响现有逻辑
部分模糊度固定             2-3周       高         假固定风险，需多判据
Bootstrapping判据          1-2周       低         纯新增判据模块
BDS码偏差改正             1-2周       低         新增改正，开关控制
GPT3+VMF3                 2-3周       中         嵌入网格数据，AOD下载
IERS2010潮汐              1周         低         Tides.java已有框架
ISB/IFCB/IFB              2-3周       中         PPP状态扩展
自适应过程噪声            1-2周       低         配置化改造
困难场景测试体系          2-3周       中         需采集/准备测试数据
精密产品下载模块          ✅ 已完成   —          rtklib-product独立模块

RTK阶段总预估：10-16周
PPP阶段总预估：5-9周

================================================================================
7. 回退安全与维护架构
================================================================================

7.1 回退安全设计原则
─────────────────────────────────────────────────────────────────────
1）所有优化通过独立Config开关控制，默认关闭
2）开关关闭时与原版RTKLIB行为完全一致
3）支持独立回退和A/B对照测试
4）新旧算法互补叠加，不替换

7.2 侵入点最小化策略
─────────────────────────────────────────────────────────────────────
1）新增算法以独立类/模块实现，通过Config开关调用
2）核心EKF循环（udstate/udobs/resamb）仅增加if分支，不修改原有逻辑
3）所有新增参数在Config中有明确默认值和文档说明

7.3 RTKLIB版本升级移植维护
─────────────────────────────────────────────────────────────────────
1）标准"化注释标记：每个优化侵入点用 // [OPT-XXX] 标注
2）代码物理隔离：新增类放在独立包中（如org.rtklib.java.ar、org.rtklib.java.tropo）
3）Config集中管理：所有优化开关在RtkConfig/PrcOpt中统一声明
4）升级时只需关注标记点，逐个确认兼容性

================================================================================
8. 现有优化项保留清单
================================================================================
以下项目独有优化项在本方案中未提及，但工程价值明确，必须保留：

1）atmFrozenNsThresh（大气冻结）：城市峡谷少星场景刚需，study未覆盖
2）enableSnrMedian（SNR中值参考星）：异构接收机网络场景有价值
3）BaseStationMonitor（基站稳定性监测）：工程运维刚需，与算法优化正交
4）rtklib-adjust P0/P1/P3（基线质量加权/残差提取/H矩阵提取）：平差模块独有

================================================================================
9. 总结
================================================================================

study.txt的优化方案方向正确、场景边界清晰、风险意识强，经交叉审核后主要修订：

1）周跳检测优先级过低：从P1提升到P0，它是所有AR策略生效的前提
2）Bootstrapping与Partial AR脱节：必须同步实现，否则Partial AR假固定风险不可控
3）GPT3/VMF3对RTK收益夸大：短基线RTK收益有限，区分RTK/PPP优先级
4）缺少困难场景测试：现有测试数据过于理想，无法验证优化项实际效果
5）IGGIII参数代码与文档不一致：需统一或说明依据
6）未覆盖项目现有独有优化：atmFrozenNsThresh、enableSnrMedian、BaseStationMonitor
   应明确纳入整体规划
7）精密产品下载模块已实现：rtklib-product独立模块，16个测试通过